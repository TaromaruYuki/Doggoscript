class Test {
    fn __constructor($a) {
        print("=== Constructor called ===")
        val $this->$a is $a
    }

    fn hello($hello) {
        print("Hello, world!")
        $this->$a is 100
        print($hello)
    }
}

val $test is Test(10)
$test->$a
$test->hello("HELP ME")
$test->$a
